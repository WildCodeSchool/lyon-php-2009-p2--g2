{% extends 'layout.html.twig' %}

{% block stylesheet %}{% endblock %}

{% block title %}
    Floor {{ game.max_floor }} Description
{% endblock %}

{% block content %}
    <div class="floorDescriptionPage">
        <h1>Floor: {{ game.max_floor }}</h1>
        <div class="floorDescriptionContainer">
            <img class="floorPicture" src="/assets/images/floorDescription/floor{{ game.max_floor }}.png" alt="Situation picture in office">
            {% if game.max_floor == 1 %}
                <p class="floorDescriptionText">Welcome to your new office, young developer! <br> You will soon realize that we are a well organized team! <br> Though, there are some events occurring... sometimes. Anyways, welcome {{ game.name }}!</p>
            {% endif %}
            {% if game.max_floor == 2 %}
                <p class="floorDescriptionText">Impressive! You managed to make it to week two! <br> and with honor if I may say so. <br> Keep it up, {{ game.name }} and you may survive... I mean, get to stay for the first month here!</p>
            {% endif %}
            {% if game.max_floor == 3 %}
                <p class="floorDescriptionText">Wow, an entire month now? <br> You're far less useless than we imagined after all... <br> Okay then, now let's see how far you can go, {{ game.name }}!</p>
            {% endif %}
            {% if game.max_floor == 4 %}
                <p class="floorDescriptionText">What can I say, {{ game.name }}? You ARE essential to us now...<br> Show us what you can do!</p>
            {% endif %}
        </div>
        <form action="/game/firstFloorEvent" method="post">
            <input type="hidden" name="id" value="{{ game.id }}">
            <div class="imageUploader">
                <div class="frameBackgroundElevator{{ game.max_floor }}">
                    <img class="avatarElevator" id="output" src="/upload/{{ game.image }}" alt="player avatar">
                </div>
            </div>
            <div class="buttonCenter">
                <button type="submit" class="fightButton">Seems EZ !<img class="buttonLogo" src="/assets/images/icones/fight.png" alt="fighting spirited person"> </button>
            </div>
        </form>
    </div>
{% endblock %}

{% block javascript %}{% endblock %}